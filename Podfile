# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Daytabase' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  pod 'XCGLogger', '~> 4.0.0'

  # Pods for Daytabase

  target 'DaytabaseTests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'Daytabase-iOS' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Daytabase-iOS

end
